import java.util.Scanner;
public class Painting // the program has the same name as the class; change the file name to match it
{
	public static void main(String[] args) // every Java program starts with a main method or function
	{
		double width, length, wallArea, ceilingArea;
		final double HEIGHT = 8;
	
		System.out.println("Calculation of Paint Requireents");
		
		length= keyboard.out.println();
		width= keyboard.out.println();
		
		public static double promptDouble (String prompt); 
			length= promptDouble("Enter room length: ");
			length= keyboard.nextDouble(); 
			
		width= promptDouble("Enther room width: ");
		width= keyboard.nextDouble();
		}

		public static double perimeter( double length, double width); 
		{	System.out.println("The Perimeter is: " + wallArea);
			wallArea=perimeter((double length, double width)* HEIGHT);
			System.out.println("The Area is: " + ceilingArea);
			ceilingArea = area(length, width);
	
		System.out.println("The wall area is " + wallArea +" square feet");
		System.out.println("The ceiling area is" + ceilingArea + "square feet.");
		
		}
	
		// write your program's Java statements here - everything else is standard Java "boilerplate"
		// TODO fill in Java statements here to implement your program - this program doesn't do anything
		// remember that every Java statement must end in a semicolon, ;, or the program won't compile
	}
 // every left curly brace, {, must have a matching right curly brace, }, or the program won't compile
